// ZIP export — placeholder for future implementation.
// Recommended approach: use the 'archiver' npm package to bundle output files.
// Requires: npm install archiver

async function createPackage(label) {
  throw new Error('ZIP export package is not yet implemented. This feature is planned for a future version.');
}

module.exports = { createPackage };
